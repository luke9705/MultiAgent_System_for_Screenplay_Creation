# Dockerfile for Audio Service (MusicGen)
FROM pytorch/pytorch:2.1.0-cuda12.1-cudnn8-runtime

WORKDIR /app

# Prevent interactive prompts during package installation
ENV DEBIAN_FRONTEND=noninteractive
ENV TZ=UTC

# Install system dependencies
RUN apt-get update && apt-get install -y \
    ffmpeg \
    libsndfile1 \
    git \
    curl \
    pkg-config \
    libavformat-dev \
    libavcodec-dev \
    libavdevice-dev \
    libavutil-dev \
    libavfilter-dev \
    libswscale-dev \
    libswresample-dev \
    && rm -rf /var/lib/apt/lists/*

# Copy requirements and install Python dependencies
COPY requirements-audio.txt .

# Install dependencies separately to bypass av==11.0.0 conflict
# 1. Install av with pre-built wheel (before audiocraft)
RUN pip install --no-cache-dir av>=14.0.0

# 2. Install audiocraft WITHOUT its dependencies (to skip av==11.0.0)
RUN pip install --no-cache-dir --no-deps audiocraft==1.3.0

# 3. Install omegaconf (required by audiocraft but skipped due to --no-deps)
RUN pip install --no-cache-dir omegaconf

# 4. Install torchaudio explicitly to match the CUDA PyTorch runtime
RUN pip install --no-cache-dir torchaudio==2.1.0

# 5. Install remaining dependencies (WITH their deps)
RUN pip install --no-cache-dir -r requirements-audio.txt

# Copy application code
COPY audio_app.py .

# Expose port
EXPOSE 7860

# Run the audio server (bind to 0.0.0.0 for Docker)
CMD ["python", "-c", "import audio_app; audio_app.demo.launch(server_name='0.0.0.0', server_port=7860, show_error=True, share=False)"]
