# Audio service - core dependencies (let pip resolve transitive deps)
--extra-index-url https://download.pytorch.org/whl/cpu

# PyTorch CPU
torch==2.1.0+cpu
torchaudio==2.1.0+cpu

# Force pre-built wheel for av FIRST (before audiocraft pulls av==11.0.0)
av>=14.0.0

# Core audio packages (install with --no-deps to skip av==11.0.0 requirement)
audiocraft==1.3.0
gradio==3.15.0

# Additional required packages
numpy==1.26.4
scipy==1.13.1
librosa
soundfile
transformers==4.36.2
huggingface-hub
einops
encodec
